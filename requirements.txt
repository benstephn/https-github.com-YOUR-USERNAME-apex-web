# The app runs on the Python standard library + Postgres in production.
# psycopg2 is only used when DATABASE_URL is set (it isn't, locally — local
# dev uses built-in SQLite, so no install is needed to run python3 server.py).
psycopg2-binary==2.9.9
